{
    "name": "evm-wallet-connector-project",
    "private": true,
    "license": "Apache-2.0",
    "scripts": {
        "build:forc": "pnpm forc build --path ./packages/signature-verification",
        "build:connector": "turbo run build",
        "build": "run-s build:forc build:connector"
    },
    "devDependencies": {
        "turbo": "^1.11.2",
        "npm-run-all": "^4.1.5"
    }
}