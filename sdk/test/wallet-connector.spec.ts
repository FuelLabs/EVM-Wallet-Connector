import { assert } from "chai";

describe("EVM Wallet Connector", () => {

    beforeEach(() => {

    });

    it("connect()", () => {
        assert.equal(true, false);
    });

    it("isConnected()", () => {
        assert.equal(true, false);
    });

    it("disconnect()", () => {
        assert.equal(true, false);
    });

    it("accounts()", () => {
        assert.equal(true, false);
    });

    it("currentAccount()", () => {
        assert.equal(true, false);
    });

    it("signMessage()", () => {
        assert.equal(true, false);
    });

    it("sendTransaction()", () => {
        assert.equal(true, false);
    });

    it("assets()", () => {
        assert.equal(true, false);
    });

    it("addAsset()", () => {
        assert.equal(true, false);
    });

    it("addAssets()", () => {
        assert.equal(true, false);
    });

    it("getWallet()", () => {
        assert.equal(true, false);
    });

    it("getProvider()", () => {
        assert.equal(true, false);
    });

    it("addAbi()", () => {
        assert.equal(true, false);
    });

    it("getAbi()", () => {
        assert.equal(true, false);
    });

    it("hasAbi()", () => {
        assert.equal(true, false);
    });

    it("network()", () => {
        assert.equal(true, false);
    });

    it("networks()", () => {
        assert.equal(true, false);
    });

    it("addNetwork()", () => {
        assert.equal(true, false);
    });

})
